[project]
name = "radarcl"
version = "0.5.0"
requires-python = ">=3.12"
license = { text = "Apache-2.0" }

[tool.pytest.ini_options]
markers = [
    "smtp: tests that require a live internet connection (DNS MX lookup, SMTP handshake, or both)",
]