[project]
name = "radarcl"
version = "0.2.0"
requires-python = ">=3.12"
license = { text = "Apache-2.0" }

[tool.pytest.ini_options]
markers = [
    "smtp: tests that require a live internet SMTP connection",
]